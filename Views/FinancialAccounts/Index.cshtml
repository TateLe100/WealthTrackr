@model IEnumerable<WealthTrackr.Models.FinancialAccount>

@{
    ViewData["Title"] = "Index";
}

<h1>All Accounts</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            @* <th>
                @Html.DisplayNameFor(model => model.FkUserId)
            </th> *@
            <th>
                Account Name
            </th>
            @* <th>
                @Html.DisplayNameFor(model => model.Balance)
            </th> *@
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            @* <td>
                @Html.DisplayFor(modelItem => item.FkUserId)
            </td> *@
            <td>
                @Html.DisplayFor(modelItem => item.AccountName)
            </td>
            @* <td>
                @Html.DisplayFor(modelItem => item.Balance)
            </td> *@
            <td>
                <a asp-action="Edit" asp-route-id="@item.FinancialAccountId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FinancialAccountId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FinancialAccountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
